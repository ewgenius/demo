name: ewgenius.demo.runtime_metrics
type: append
migrations:
  - name: create_table
    sql: |
      CREATE TABLE IF NOT EXISTS ewgenius.demo.runtime_metrics (
        timestamp BIGINT,
        name TEXT,
        value FLOAT,
        labels TEXT
      );
