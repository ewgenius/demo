name: eth.recent_transactions_view_public
type: append
migrations:
  - name: create_view
    sql: |
      CREATE VIEW eth.recent_transactions_view AS
        SELECT * FROM eth.recent_transactions;
visibility: public # global | public | private